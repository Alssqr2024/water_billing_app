# 💧 نظام فواتير المياه (Water Billing App)

تطبيق Flutter متكامل لإدارة فواتير المياه والمشتركين، مبني بهندسة معمارية نظيفة (Clean Architecture) مع إدارة حالة متقدمة باستخدام Riverpod.

## 📋 نظرة عامة

نظام فواتير المياه هو تطبيق محمول شامل يهدف إلى تسهيل إدارة المشتركين وفواتير استهلاك المياه. يوفر التطبيق واجهة مستخدم عربية سهلة الاستخدام مع إمكانيات متقدمة لتتبع القراءات والفواتير والمدفوعات.

### ✨ المميزات الرئيسية

- 👥 **إدارة المشتركين**: إضافة، تعديل، حذف، والبحث عن المشتركين
- 🧾 **إدارة الفواتير**: إنشاء فواتير جديدة، تتبع حالة الدفع، وعرض سجل الفواتير
- 📊 **لوحة تحكم شاملة**: إحصائيات فورية عن المشتركين والفواتير
- 📄 **طباعة الفواتير**: إنشاء فواتير PDF احترافية قابلة للطباعة والمشاركة
- ⚙️ **الإعدادات**: تخصيص سعر الوحدة وإعدادات النظام
- 💾 **قاعدة بيانات محلية**: تخزين البيانات محلياً باستخدام SQLite
- 🌙 **واجهة عربية**: دعم كامل للغة العربية مع خط Cairo

## 🏗️ الهندسة المعمارية

تم بناء التطبيق باستخدام **Clean Architecture** (الهندسة المعمارية النظيفة) لضمان:
- فصل واضح بين طبقات التطبيق
- سهولة الصيانة والتوسع
- قابلية الاختبار العالية
- استقلالية الأكواد

### 📁 هيكل المشروع

```
lib/
├── core/                          # المكونات الأساسية المشتركة
│   ├── database/                  # إدارة قاعدة البيانات
│   │   └── database_helper.dart   # SQLite helper
│   ├── utils/                     # أدوات مساعدة
│   └── widgets/                   # واجهات مشتركة
│
├── features/                      # الميزات الرئيسية
│   ├── customers/                 # ميزة إدارة المشتركين
│   │   ├── data/                  # طبقة البيانات
│   │   │   ├── data_source/       # مصادر البيانات (SQLite)
│   │   │   ├── models/            # نماذج البيانات
│   │   │   └── repositories/      # تطبيق المستودعات
│   │   ├── domain/                # طبقة الأعمال
│   │   │   ├── entities/          # كيانات الأعمال
│   │   │   ├── repositories/      # واجهات المستودعات
│   │   │   └── usecases/          # حالات الاستخدام
│   │   └── presentation/          # طبقة العرض
│   │       ├── pages/             # الصفحات
│   │       ├── providers/         # Riverpod providers
│   │       └── widgets/           # واجهات مخصصة
│   │
│   ├── bills/                     # ميزة إدارة الفواتير
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   │
│   └── settings/                  # ميزة الإعدادات
│       ├── data/
│       ├── domain/
│       └── presentation/
│
├── main.dart                      # نقطة البداية
└── home.dart                      # الصفحة الرئيسية
```

### 🔄 طبقات الهندسة المعمارية

#### 1️⃣ طبقة العرض (Presentation Layer)
- **المسؤولية**: عرض الواجهات والتفاعل مع المستخدم
- **المكونات**:
  - `Pages`: صفحات التطبيق
  - `Widgets`: مكونات واجهة المستخدم القابلة لإعادة الاستخدام
  - `Providers`: إدارة الحالة باستخدام Riverpod

#### 2️⃣ طبقة الأعمال (Domain Layer)
- **المسؤولية**: منطق الأعمال الأساسي
- **المكونات**:
  - `Entities`: كيانات الأعمال النقية
  - `Repositories`: واجهات المستودعات (Interfaces)
  - `Use Cases`: حالات الاستخدام (كل عملية في ملف منفصل)

#### 3️⃣ طبقة البيانات (Data Layer)
- **المسؤولية**: التعامل مع مصادر البيانات
- **المكونات**:
  - `Data Sources`: الاتصال بقاعدة البيانات المحلية
  - `Models`: نماذج البيانات مع دوال التحويل
  - `Repository Implementations`: تطبيق واجهات المستودعات

## 🛠️ التقنيات والأدوات المستخدمة

### 📦 الحزم الرئيسية

| الحزمة | الإصدار | الاستخدام |
|--------|---------|-----------|
| `flutter_riverpod` | ^2.4.9 | إدارة الحالة (State Management) |
| `sqflite` | ^2.3.0 | قاعدة بيانات SQLite المحلية |
| `path` | ^1.8.3 | إدارة مسارات الملفات |
| `get` | ^4.6.6 | التنقل وإدارة الحالة المساعدة |
| `pdf` | ^3.10.4 | إنشاء ملفات PDF |
| `printing` | ^5.10.2 | طباعة ومشاركة ملفات PDF |
| `share_plus` | ^7.0.0 | مشاركة الملفات |
| `path_provider` | ^2.1.1 | الوصول إلى مسارات النظام |

### 🎨 التصميم

- **Material Design 3**: تصميم حديث ومتجاوب
- **خط Cairo**: خط عربي احترافي وواضح
- **ألوان متناسقة**: نظام ألوان أزرق احترافي

## 🚀 البدء مع المشروع

### المتطلبات الأساسية

- Flutter SDK (3.9.2 أو أحدث)
- Dart SDK
- محرر أكواد (VS Code أو Android Studio)
- محاكي أو جهاز فعلي للاختبار

### 📥 التثبيت والتشغيل

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd water_billing_app
```

2. **تثبيت الحزم**
```bash
flutter pub get
```

3. **تشغيل التطبيق**
```bash
flutter run
```

### 🔧 البناء للإنتاج

**Android:**
```bash
flutter build apk --release
```

**iOS:**
```bash
flutter build ios --release
```

**Windows:**
```bash
flutter build windows --release
```

## 📱 الميزات التفصيلية

### 1. إدارة المشتركين
- ➕ إضافة مشترك جديد (الاسم، رقم الهاتف)
- ✏️ تعديل بيانات المشترك
- 🗑️ حذف مشترك (مع حذف جميع فواتيره)
- 🔍 البحث عن المشتركين
- 📋 عرض قائمة جميع المشتركين

### 2. إدارة الفواتير
- 📝 إنشاء فاتورة جديدة
  - اختيار المشترك
  - إدخال القراءة السابقة والحالية
  - حساب الاستهلاك تلقائياً
  - حساب المبلغ الإجمالي
- 💰 تحديث حالة الدفع (مدفوع/غير مدفوع)
- 📄 طباعة الفاتورة كملف PDF
- 📤 مشاركة الفاتورة
- 🗑️ حذف الفاتورة
- 📊 عرض فواتير مشترك معين
- 📈 إحصائيات الفواتير (المدفوعة/غير المدفوعة)

### 3. لوحة التحكم الرئيسية
- 📊 إحصائيات فورية:
  - إجمالي عدد المشتركين
  - إجمالي عدد الفواتير
  - عدد الفواتير المدفوعة
  - عدد الفواتير غير المدفوعة
- ⚡ إجراءات سريعة:
  - إضافة مشترك جديد
  - إنشاء فاتورة جديدة
- 📋 عرض أحدث الفواتير

### 4. الإعدادات
- 💵 تحديث سعر الوحدة (متر مكعب)
- ℹ️ معلومات التطبيق

## 🗄️ قاعدة البيانات

### جداول قاعدة البيانات

#### جدول المشتركين (customers)
```sql
CREATE TABLE customers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  phone TEXT NOT NULL
)
```

#### جدول الفواتير (bills)
```sql
CREATE TABLE bills (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  customer_id INTEGER NOT NULL,
  customer_name TEXT NOT NULL,
  previous_reading REAL NOT NULL,
  current_reading REAL NOT NULL,
  consumption REAL NOT NULL,
  unit_price REAL NOT NULL,
  total_amount REAL NOT NULL,
  bill_date INTEGER NOT NULL,
  is_paid INTEGER NOT NULL DEFAULT 0,
  FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE
)
```

#### جدول الإعدادات (settings)
```sql
CREATE TABLE settings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT UNIQUE NOT NULL,
  value TEXT NOT NULL
)
```

## 🎯 حالات الاستخدام (Use Cases)

### المشتركين
- `AddCustomerUseCase`: إضافة مشترك جديد
- `GetCustomersUseCase`: جلب جميع المشتركين
- `UpdateCustomerUseCase`: تحديث بيانات مشترك
- `DeleteCustomerUseCase`: حذف مشترك

### الفواتير
- `AddBillUseCase`: إضافة فاتورة جديدة
- `GetBillsUseCase`: جلب جميع الفواتير
- `GetBillByIdUseCase`: جلب فاتورة محددة
- `GetCustomerBillsUseCase`: جلب فواتير مشترك معين
- `UpdateBillUseCase`: تحديث فاتورة
- `DeleteBillUseCase`: حذف فاتورة
- `MarkBillAsPaidUseCase`: تحديد فاتورة كمدفوعة
- `MarkBillAsUnpaidUseCase`: تحديد فاتورة كغير مدفوعة

### الإعدادات
- `UpdateUnitPriceUseCase`: تحديث سعر الوحدة

## 🔄 إدارة الحالة (State Management)

يستخدم التطبيق **Riverpod** لإدارة الحالة بشكل فعال:

- `customerProvider`: إدارة حالة المشتركين
- `billProvider`: إدارة حالة الفواتير
- `settingsProvider`: إدارة حالة الإعدادات

## 📸 لقطات الشاشة

> يمكن إضافة لقطات شاشة للتطبيق هنا

## 🤝 المساهمة

المساهمات مرحب بها! يرجى اتباع الخطوات التالية:

1. Fork المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 📧 التواصل

لأي استفسارات أو اقتراحات، يرجى فتح Issue في المستودع.

---

**تم البناء بـ ❤️ باستخدام Flutter**
